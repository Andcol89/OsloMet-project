// Mal for konfigurasjon. Kopier denne filen til config.js og fyll inn riktig URL.
// config.js skal IKKE committes til git (se .gitignore).

window.APP_CONFIG = {
  FLOW_URL: "DIN_POWER_AUTOMATE_URL_HER"
};
